{
  "name": "pr-gpt-summary-action",
  "version": "1.0.0",
  "description": "PR 요약 GPT 액션",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^5.1.1",
    "openai": "^4.44.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.0"
  },
  "engines": {
    "node": ">=16"
  }
}
